package X2CityComponent_EnergyPath {
    private import X2CitySharedContracts::*;
    part def EnergyPath /* Provides the physical high-energy path between the battery source and vehicle loads. This hardware boundary carries energy; it does not define policy behavior. */; // LE-ENERGY-PATH
}
