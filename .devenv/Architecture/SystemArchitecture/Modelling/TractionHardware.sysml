package X2CityComponent_TractionHardware {
    private import X2CitySharedContracts::*;
    part def TractionHardware /* Provides the physical propulsion drive and torque actuation function. This supplied hardware role executes commands from motor control; project software does not implement the hardware here. */; // LE-TRACTION-HW
}
